base0:
  build: ../base
base1:
  build: ../base
front:
  build: ../front
frontend:
  build: ../register
backend:
  build: ../admin
api:
  build: ../api
student:
  build: ../student
student_store:
  build: ../student-store




