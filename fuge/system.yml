base0:
  build: ../base
base1:
  build: ../base
front:
  build: ../front
api:
  build: ../api
post:
  build: ../student
post:
  build: ../post
entry_store:
  build: ../entry-store
reserve:
  build: ../reserve




